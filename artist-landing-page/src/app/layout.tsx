import type { Metadata } from "next";
import { Geist } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Kio Inganga — Artist",
  description:
    "Official portfolio of Kio Inganga — visual artist, creative visionary. Explore original works, commissions, and exhibitions.",
  openGraph: {
    title: "Kio Inganga — Artist",
    description:
      "Visual artist and creative visionary. Explore original works and commissions.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col bg-[var(--background)] text-[var(--foreground)]">
        {children}
      </body>
    </html>
  );
}
